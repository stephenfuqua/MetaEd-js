{
  "name": "metaed-js",
  "main": "dist/index.js",
  "version": "1.0.0-dev.5",
  "description": "MetaEd for JavaScript",
  "license": "private",
  "publishConfig": {
    "registry": "https://www.myget.org/F/ed-fi/npm/"
  },
  "dependencies": {
    "antlr4": "4.6",
    "final-fs": "^1.6.1",
    "handlebars": "^4.0.6",
    "immutable": "^3.8.1",
    "ramda": "^0.22.1",
    "safety-lens": "^1.5.0",
    "winston": "^2.3.0",
    "yargs": "^6.5.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-eslint": "^7.1.1",
    "babel-plugin-transform-flow-strip-types": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-register": "^6.18.0",
    "chai": "^3.5.0",
    "eslint": "^3.11.0",
    "eslint-config-airbnb-base": "^10.0.1",
    "eslint-plugin-flowtype": "^2.28.2",
    "eslint-plugin-import": "^2.2.0",
    "flow-bin": "^0.39.0",
    "flow-typed": "^2.0.0",
    "mocha": "^2.5.3",
    "mock-fs": "^3.12.1",
    "rimraf": "^2.6.1",
    "standard-version": "^4.0.0"
  },
  "scripts": {
    "test": "mocha --compilers js:babel-register \"test/**/*Tests.js\" \"test-integration/**/*Tests.js\"",
    "build": "rimraf dist/ && babel src -d dist --source-maps",
    "lint": "yarn run flow && yarn run eslint && yarn run eslint:tests",
    "flow": "flow check",
    "eslint": "eslint src",
    "eslint:tests": "eslint test && eslint test-integration",
    "release:version-dev": "standard-version --prerelease dev",
    "release:push-tags": "git push --follow-tags",
    "release:publish-dev": "yarn run build && yarn test && yarn run lint && yarn run release:push-tags && npm publish --tag=dev"
  }
}
