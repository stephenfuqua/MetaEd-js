{
  "name": "metaed-odsapi-deploy",
  "main": "dist/index.js",
  "version": "1.3.2-dev.11",
  "description": "MetaEd for JavaScript",
  "license": "SEE LICENSE IN LICENSE.md",
  "publishConfig": {
    "registry": "https://www.myget.org/F/ed-fi/npm/"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "chalk": "^2.3.0",
    "fs-extra": "^5.0.0",
    "metaed-core": "1.3.2-dev.11",
    "ramda": "^0.25.0",
    "semver": "^5.4.1",
    "sugar": "^2.0.4",
    "touch": "^3.1.0",
    "winston": "3.0.0",
    "yargs": "^8.0.2"
  },
  "scripts": {
    "build": "yarn build:clean && yarn build:dist && yarn build:flow",
    "build:clean": "rimraf dist",
    "build:flow": "flow-copy-source -v src dist",
    "build:dist": "rimraf dist && babel src -d dist --source-maps --copy-files"
  }
}
