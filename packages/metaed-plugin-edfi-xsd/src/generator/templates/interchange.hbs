<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema" xmlns="http://ed-fi.org/{{{schemaVersion}}}" targetNamespace="http://ed-fi.org/{{{schemaVersion}}}" elementFormDefault="qualified" attributeFormDefault="unqualified">
  <xs:include schemaLocation="{{{interchange.schemaLocation}}}"/>
  <xs:annotation>
    <xs:documentation>===== {{{interchange.humanizedUppercasedMetaEdName}}} Interchange Model =====</xs:documentation>
  </xs:annotation>
  <xs:element name="{{{interchange.interchangeName}}}">
    <xs:annotation>
      <xs:documentation>{{{interchange.documentation}}}</xs:documentation>
    </xs:annotation>
    <xs:complexType>
      <xs:choice maxOccurs="unbounded">
        {{#interchange.orderedElements}}
          <xs:element name="{{{xsd_Name}}}" type="{{{xsd_Type}}}"/>
        {{/interchange.orderedElements}}
        </xs:choice>
    </xs:complexType>
  </xs:element>
</xs:schema>
